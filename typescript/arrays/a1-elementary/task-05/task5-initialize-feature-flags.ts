/**
 * Task 5: Initialize Feature Flags
 * Read the requirements in task-05.md
 */

export function getFeatureFlags(): boolean[] {
    // TODO: Declare, strictly type, and initialize an array of booleans 
    let featureFlags: boolean[] = [true, false, true];
    // containing exactly: true, false, and true (in that exact order).
    // Return that array.
    return featureFlags;
    
    
}